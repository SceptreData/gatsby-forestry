---
label: Menu
hide_body: true
fields:
- name: menu_item
  type: field_group_list
  fields:
  - name: label
    type: text
    config:
      required: true
      min: '3'
    label: Label
  - name: url
    type: text
    config:
      required: false
    label: URL
    description: Where do you want this link to go?
  - name: list_order
    type: list
    config:
      use_select: false
      min: 
      max: 
    label: List Order
  config:
    min: 
    max: 
    labelField: 
  label: Menu Item
